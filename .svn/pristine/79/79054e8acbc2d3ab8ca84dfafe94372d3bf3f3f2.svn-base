use day14;
create table user(
	username varchar(100) primary key,
	nick varchar(100),
	password varchar(100),
	email varchar(100),
	birthday date
);

// UserDaoMySQLImpl实现数据库的操作
// insert into user (username,nick,password,email,birthday) values(?,?,?,?,?)